import { apple, grocery, home, loan, moneyTransfer, myCards, receive, search, send, settings, spotify, statistics, topUp } from "./icons";

import { card, profile } from "./images";

export const icons = {
    apple,
    grocery,
    home,
    loan,
    moneyTransfer,
    myCards,
    receive,
    search,
    send,
    settings,
    spotify,
    statistics,
    topUp,
}

export const images = {
    card,
    profile,
}
